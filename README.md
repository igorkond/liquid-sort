# liquid-sort
A liquid sort puzzle solver

# Description


# How to use
Pass a N×V array (i.e. an array of N arrays, each with up to V items that can be numbers or strings) as input to the findMoves(input) function, then log its return value with console.log. Its return value should be a sequence of moves (as an array of \[indexOfInnerArrayFromWhichToMove, indexOfInnerArrayToWhichToMove\] arrays) to sort the provided input.

# Installation
git clone https://github.com/igorkond/liquid-sort
cd liquid-sort
npm install
